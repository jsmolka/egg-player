#ifndef TYPES_HPP
#define TYPES_HPP

#include <QString>
#include <QVector>

using File = QString;
using Path = QString;

using Files = QVector<QString>;
using Paths = QVector<QString>;

#endif // TYPES_HPP
